#include <bits/stdc++.h>
using namespace std;

#define FAST ios_base::sync_with_stdio(0); cin.tie(NULL);
typedef long long ll;

int main(){
    int t;
    cin >> t;
    while(t--){
        vector<ll> v;
        ll stop;
        cin >> stop;
        while (stop--)
        {
            ll tmp;
            cin >> tmp;
            v.push_back(tmp);
        }
        
        
    }
    return 0;
}