#include <bits/stdc++.h>
using namespace std;

#define FAST ios_base::sync_with_stdio(0); cin.tie(NULL);

int main(){
    int a, b;
    cin >> a >> b;
    cout << (int)(a*b)/2 << endl;
    return 0;
}